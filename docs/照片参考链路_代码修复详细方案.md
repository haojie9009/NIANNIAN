# 代码修复详细方案 — 照片参考链路

## 改动总览

| # | 文件 | 改动类型 | 说明 |
|---|---|---|---|
| 1 | `backend/routers/assets.py` | 修改 | 上传接口接收 `subject` + `period_label` 字段 |
| 2 | `backend/services/service_manager.py` | 修改 | 新增 `_select_reference_photo()` + 修改 `gen_scene_image()` |
| 3 | `backend/routers/pipeline.py` | 修改 | `scene_image` 路由接收 `reference_photo_url` |
| 4 | `frontend/memorial.html` | 修改 | 上传区域改为卡片列表，支持选择主体和时期 |
| 5 | `frontend/js/memorial.js` | 修改 | `handleUpload()` 改为逐个文件弹窗选择后上传 |
| 6 | `frontend/js/studio.js` | 修改 | `genSceneImage()` 获取已上传照片并发送参考图 URL |
| 7 | `frontend/css/style.css` | 新增 | 上传卡片样式 + 选择器样式 |

---

## 改动 1：`backend/routers/assets.py` — 扩展上传接口

**当前代码（第 18-43 行）**：

```python
@router.post("/upload")
async def upload(
    session_id: str = Form(...),
    period: str = Form("default"),
    file: UploadFile = File(...),
) -> Dict[str, Any]:
```

**改为**：

```python
@router.post("/upload")
async def upload(
    session_id: str = Form(...),
    period: str = Form("default"),
    subject: str = Form("deceased"),       # 新增：deceased | family | group | other
    period_label: str = Form(""),           # 新增：青年 | 中年 | 老年 | 空
    file: UploadFile = File(...),
) -> Dict[str, Any]:
    try:
        s = session_store.require(session_id)
    except KeyError:
        raise HTTPException(404, "session not found")

    suffix = Path(file.filename or "").suffix.lower()
    if suffix not in _ALLOWED_EXT:
        raise HTTPException(400, f"unsupported file type: {suffix}")

    fname = f"{session_id}_{period}_{uuid.uuid4().hex[:8]}{suffix}"
    fpath = sm.UPLOADS_DIR / fname
    fpath.write_bytes(await file.read())

    s["assets"].append({
        "filename":     file.filename,
        "saved_as":     fname,
        "period":       period,
        "subject":      subject,      # 新增
        "period_label": period_label, # 新增
        "url":          f"/api/assets/file/{fname}",
    })
    return {"ok": True, "asset": s["assets"][-1], "total": len(s["assets"])}
```

**关键点**：
- `subject` 默认 `"deceased"`，保证向后兼容（老客户端不传时自动标记为逝者）
- `period_label` 默认 `""`（空字符串），不强制要求

---

## 改动 2：`backend/services/service_manager.py` — 智能匹配 + 传入参考图

### 2.1 新增 `_select_reference_photo()` 函数

**插入位置**：`gen_scene_image()` 函数之前（约第 674 行之前）

```python
def _select_reference_photo(s: dict, scene: dict, scene_idx: int) -> Optional[str]:
    """根据分镜内容和场景时期，从用户上传的照片中选择最合适的一张作为参考。

    优先级：
    1. 分镜中明确指定的照片（通过 scene["_reference_photo"] 由前端指定）
    2. 根据分镜时期自动匹配（如"年轻时"→ period_label="青年" 的照片）
    3. subject="deceased" 的第一张照片
    4. 任意照片（如果没有逝者照片）
    """
    assets = s.get("assets", [])
    # 只取图片，不包含视频
    image_exts = ('.jpg', '.jpeg', '.png', '.webp', '.gif')
    photos = [a for a in assets if a.get("filename", "").lower().endswith(image_exts)]

    if not photos:
        return None

    # 1. 前端指定（URL 匹配 saved_as）
    specified = scene.get("_reference_photo")
    if specified:
        match = next((p for p in photos if p.get("url") == specified), None)
        if match:
            return match["saved_as"]

    # 2. 时期匹配（根据分镜描述中的关键词匹配时期）
    scene_desc = (scene.get("description") or scene.get("visual") or "").lower()
    period_keywords = {
        "青年": ["青年", "年轻", "大学", "青春", "插队", "知青"],
        "中年": ["中年", "工作", "车间", "技术", "工程师"],
        "老年": ["老年", "退休", "晚年", "白发"],
    }

    for label, keywords in period_keywords.items():
        if any(kw in scene_desc for kw in keywords):
            match = next(
                (p for p in photos if p.get("period_label") == label and p.get("subject") == "deceased"),
                None,
            )
            if match:
                return match["saved_as"]

    # 3. 默认：逝者的第一张照片
    deceased_photo = next((p for p in photos if p.get("subject") == "deceased"), None)
    if deceased_photo:
        return deceased_photo["saved_as"]

    # 4. 兜底：第一张照片
    return photos[0]["saved_as"]
```

### 2.2 修改 `gen_scene_image()` 函数签名和调用

**当前（第 675 行）**：
```python
def gen_scene_image(sid: str, scene_idx: int) -> Dict[str, Any]:
```

**改为**：
```python
def gen_scene_image(sid: str, scene_idx: int, reference_photo_url: str = "") -> Dict[str, Any]:
```

**当前（第 713 行）**：
```python
    b64, err = generate_image_302(image_prompt)
```

**改为**：
```python
    # ── 选择参考照片 ──
    ref_saved_as = reference_photo_url or _select_reference_photo(s, scene, scene_idx)
    reference_b64 = None
    if ref_saved_as:
        asset_path = UPLOADS_DIR / ref_saved_as
        if asset_path.exists():
            import base64
            reference_b64 = base64.b64encode(asset_path.read_bytes()).decode()

    b64, err = generate_image_302(image_prompt, reference_b64=reference_b64)
```

**当前（第 719-721 行）**：
```python
    scene["_image_url"] = local_url
    scene["_image_prompt"] = image_prompt
    session_store.update(sid)
    return {"url": local_url}
```

**在 `scene["_image_prompt"] = image_prompt` 后新增一行**：
```python
    scene["_image_url"] = local_url
    scene["_image_prompt"] = image_prompt
    scene["_reference_photo"] = ref_saved_as or ""
    session_store.update(sid)
```

**Playback 缓存逻辑注意事项**：
Playback 模式下的缓存检查在第 686-701 行，逻辑不变。但 playback 模式下如果需要考虑参考照片变化导致的缓存失效，可以后续添加 `_reference_photo` 字段到缓存 key。当前版本不需要处理。

---

## 改动 3：`backend/routers/pipeline.py` — 路由接收参数

**当前（第 100-107 行）**：
```python
@router.post("/scene/image/{sid}/{idx}")
def scene_image(sid: str, idx: int) -> Dict[str, Any]:
    """为单个分镜生成首帧图片（返回 data URL）"""
    try:
        session_store.require(sid)
    except KeyError:
        raise HTTPException(404, "session not found")
    return sm.gen_scene_image(sid, idx)
```

**改为**：
```python
@router.post("/scene/image/{sid}/{idx}")
def scene_image(sid: str, idx: int, payload: Optional[Dict[str, Any]] = Body(None)) -> Dict[str, Any]:
    """为单个分镜生成首帧图片（返回 data URL）"""
    try:
        session_store.require(sid)
    except KeyError:
        raise HTTPException(404, "session not found")
    reference_photo_url = (payload or {}).get("reference_photo_url", "")
    return sm.gen_scene_image(sid, idx, reference_photo_url)
```

---

## 改动 4：`frontend/memorial.html` — 上传 UI 改为卡片列表

**当前（第 128-136 行）**：
```html
<div class="card">
  <div class="section-label">照片上传（选填）</div>
  <label class="upload-zone" for="upload_input">
    点击或拖拽上传照片 / 视频<br/>
    <span class="text-muted">支持多文件，可上传不同时期的照片</span>
  </label>
  <input type="file" id="upload_input" class="upload-input" multiple accept="image/*,video/*">
  <div class="upload-list" id="uploadList"></div>
</div>
```

**改为**：
```html
<div class="card">
  <div class="section-label">照片上传（选填）</div>
  <label class="upload-zone" for="upload_input">
    点击或拖拽上传照片 / 视频<br/>
    <span class="text-muted">支持多文件，可上传不同时期的照片</span>
  </label>
  <input type="file" id="upload_input" class="upload-input" multiple accept="image/*,video/*">
  <div class="upload-list" id="uploadList"></div>
  <!-- 照片选择模板（隐藏，由 JS 动态使用） -->
  <template id="tplPhotoSelector">
    <select class="photo-select-subject">
      <option value="deceased" selected>逝者本人</option>
      <option value="family">家属/亲友</option>
      <option value="group">合照/多人</option>
      <option value="other">其他</option>
    </select>
    <select class="photo-select-period">
      <option value="" selected>时期不确定</option>
      <option value="青年">青年时期</option>
      <option value="中年">中年时期</option>
      <option value="老年">老年时期</option>
    </select>
  </template>
</div>
```

---

## 改动 5：`frontend/js/memorial.js` — 上传逻辑重写

**当前 `handleUpload()` 函数（第 193-214 行）**：

```javascript
async function handleUpload(files) {
  const sid = getSessionId();
  if (!sid) { toast('请先填写基本信息'); return; }
  const list = document.getElementById('uploadList');
  for (const f of files) {
    const fd = new FormData();
    fd.append('session_id', sid);
    fd.append('period', 'default');
    fd.append('file', f);
    try {
      const res = await apiUpload('/assets/upload', fd);
      if (f.type.startsWith('image/')) {
        const url = URL.createObjectURL(f);
        list.insertAdjacentHTML('beforeend', `<img class="upload-thumb" src="${url}" alt="">`);
      } else {
        list.insertAdjacentHTML('beforeend', `<div class="upload-thumb" style="...">${esc(f.name.slice(0, 8))}</div>`);
      }
    } catch (e) { toast('上传失败：' + e.message); }
  }
  toast('上传完成');
}
```

**改为**：

```javascript
// ───── 文件上传 ─────
async function handleUpload(files) {
  const sid = getSessionId();
  if (!sid) { toast('请先填写基本信息'); return; }
  const list = document.getElementById('uploadList');

  for (const f of files) {
    const isImage = f.type.startsWith('image/');
    const isVideo = f.type.startsWith('video/');
    if (!isImage && !isVideo) continue;

    // 创建卡片容器
    const card = document.createElement('div');
    card.className = 'upload-card';

    // 预览
    const thumb = document.createElement(isImage ? 'img' : 'div');
    if (isImage) {
      thumb.className = 'upload-thumb';
      thumb.src = URL.createObjectURL(f);
    } else {
      thumb.className = 'upload-thumb video-thumb';
      thumb.textContent = '▶ ' + esc(f.name.slice(0, 10));
    }
    card.appendChild(thumb);

    // 上传中状态
    const status = document.createElement('div');
    status.className = 'upload-card-status';
    status.textContent = '上传中...';
    card.appendChild(status);

    list.appendChild(card);

    // 上传文件
    const fd = new FormData();
    fd.append('session_id', sid);
    fd.append('period', 'default');
    fd.append('subject', 'deceased');  // 默认值
    fd.append('period_label', '');
    fd.append('file', f);

    try {
      const res = await apiUpload('/assets/upload', fd);
      const asset = res.asset;

      status.textContent = '已上传 ✓';

      // 仅图片显示选择器
      if (isImage) {
        const selectors = document.createElement('div');
        selectors.className = 'upload-card-selectors';

        const subjectSel = document.createElement('select');
        subjectSel.className = 'photo-select-subject';
        subjectSel.innerHTML = `
          <option value="deceased" ${asset.subject === 'deceased' ? 'selected' : ''}>逝者本人</option>
          <option value="family" ${asset.subject === 'family' ? 'selected' : ''}>家属/亲友</option>
          <option value="group" ${asset.subject === 'group' ? 'selected' : ''}>合照/多人</option>
          <option value="other" ${asset.subject === 'other' ? 'selected' : ''}>其他</option>
        `;

        const periodSel = document.createElement('select');
        periodSel.className = 'photo-select-period';
        periodSel.innerHTML = `
          <option value="" ${!asset.period_label ? 'selected' : ''}>时期不确定</option>
          <option value="青年" ${asset.period_label === '青年' ? 'selected' : ''}>青年时期</option>
          <option value="中年" ${asset.period_label === '中年' ? 'selected' : ''}>中年时期</option>
          <option value="老年" ${asset.period_label === '老年' ? 'selected' : ''}>老年时期</option>
        `;

        // 修改选择时，更新后端记录
        async function updateAssetMeta() {
          // 通过 PATCH 更新 asset（需要新增接口，或使用重新上传）
          // 简化方案：存储到本地，生成时由前端传递 URL
          card.dataset.subject = subjectSel.value;
          card.dataset.period = periodSel.value;
          card.dataset.url = asset.url;
        }
        subjectSel.onchange = updateAssetMeta;
        periodSel.onchange = updateAssetMeta;

        selectors.appendChild(subjectSel);
        selectors.appendChild(periodSel);
        card.appendChild(selectors);

        // 初始化 dataset
        card.dataset.subject = asset.subject || 'deceased';
        card.dataset.period = asset.period_label || '';
        card.dataset.url = asset.url;
      }
    } catch (e) {
      status.textContent = '上传失败';
      status.style.color = 'var(--red)';
      toast('上传失败：' + e.message);
    }
  }
  toast('上传完成');
}
```

**简化方案说明**：上面的选择器 onchange 只更新本地 dataset，不实时回传后端。原因是后端 assets 是 list append 结构，没有按索引 patch 的接口。有两种处理方式：

**方案 A（推荐，简单）**：选择器值只存前端 dataset，在跳转 Step 3 时一次性提交所有 asset 的 subject/period_label。需要在 `gotoStep3()` 中增加：

```javascript
async function gotoStep3() {
  // ... 原有逻辑 ...

  // 新增：更新所有照片的 subject 和 period_label
  const cards = document.querySelectorAll('#uploadList .upload-card[data-url]');
  for (const card of cards) {
    const fd = new FormData();
    fd.append('session_id', sid);
    fd.append('asset_url', card.dataset.url);
    fd.append('subject', card.dataset.subject || 'deceased');
    fd.append('period_label', card.dataset.period || '');
    await apiPost('/assets/update-meta', fd);
  }

  // ... 原有 gotoStep3 逻辑继续 ...
}
```

**方案 B（更简单，不需要新接口）**：前端不存 dataset，studio.js 生成分镜时直接从 `/assets/list/{sid}` 读取最新的 subject/period_label（这时用户已经在 memorial.html 中选择了），后端 assets 的 subject/period_label 需要在上传时就确定。

考虑到实现复杂度，**推荐方案 B 的变体**：上传时让用户先选默认值（deceased + 空时期），上传后用户可以改，改动通过一个新接口 `/assets/update-meta` 提交。

**因此需要新增一个后端接口**（见下方）。

### 5.1 新增接口：`POST /assets/update-meta`

在 `backend/routers/assets.py` 中新增：

```python
@router.post("/update-meta")
async def update_meta(
    session_id: str = Form(...),
    asset_url: str = Form(...),
    subject: str = Form("deceased"),
    period_label: str = Form(""),
) -> Dict[str, Any]:
    try:
        s = session_store.require(session_id)
    except KeyError:
        raise HTTPException(404, "session not found")

    for asset in s["assets"]:
        if asset.get("url") == asset_url:
            asset["subject"] = subject
            asset["period_label"] = period_label
            session_store.update(session_id)
            return {"ok": True}

    raise HTTPException(404, "asset not found")
```

---

## 改动 6：`frontend/js/studio.js` — 发送参考照片 URL

**当前 `genSceneImage()` 函数（第 197-216 行）**：

```javascript
async function genSceneImage(idx) {
  const sc = state.scenes[idx];
  if (!sc) return;
  sc._img_status = 'run';
  renderScenes();
  setPill('MV05', 'active');
  try {
    const res = await apiPost(`/pipeline/scene/image/${state.sid}/${idx}`, {});
    if (res.error) throw new Error(res.message || '图片生成失败');
    sc._img_url    = res.url || res.image_url;
    sc._img_status = 'done';
    sc._img_loaded = true;
  } catch (e) {
    sc._img_status = 'err';
    toast('图片生成失败：' + e.message);
  } finally {
    renderScenes();
    if (state.scenes.length && state.scenes.every(s => s._img_url)) setPill('MV05', 'done');
  }
}
```

**改为**：

```javascript
async function genSceneImage(idx) {
  const sc = state.scenes[idx];
  if (!sc) return;

  // 获取用户上传的照片列表（仅图片，不含视频）
  let reference_photo_url = '';
  try {
    const assetsRes = await apiGet(`/assets/list/${state.sid}`);
    const photos = (assetsRes.assets || []).filter(a =>
      a.filename && a.filename.match(/\.(jpg|jpeg|png|webp|gif)$/i)
    );
    if (photos.length > 0) {
      // 优先使用标记为"逝者"的照片
      const deceased = photos.find(p => p.subject === 'deceased');
      reference_photo_url = (deceased || photos[0]).url;
    }
  } catch (e) {
    console.warn('[genSceneImage] 获取照片列表失败：', e.message);
  }

  sc._img_status = 'run';
  renderScenes();
  setPill('MV05', 'active');
  try {
    const payload = {};
    if (reference_photo_url) {
      payload.reference_photo_url = reference_photo_url;
    }
    const res = await apiPost(`/pipeline/scene/image/${state.sid}/${idx}`, payload);
    if (res.error) throw new Error(res.message || '图片生成失败');
    sc._img_url    = res.url || res.image_url;
    sc._img_status = 'done';
    sc._img_loaded = true;
  } catch (e) {
    sc._img_status = 'err';
    toast('图片生成失败：' + e.message);
  } finally {
    renderScenes();
    if (state.scenes.length && state.scenes.every(s => s._img_url)) setPill('MV05', 'done');
  }
}
```

---

## 改动 7：`frontend/css/style.css` — 新增样式

**在 `.upload-thumb` 样式后（约第 145 行后）新增**：

```css
/* 上传卡片（带选择器） */
.upload-card{
  display:flex;flex-direction:column;gap:6px;
  width:120px;padding:8px;border-radius:10px;
  border:1px solid var(--border);background:var(--surf2);
}
.upload-card-status{font-size:.7rem;color:var(--green);}
.upload-card-selectors{display:flex;flex-direction:column;gap:4px;}
.upload-card-selectors select{
  width:100%;font-size:.7rem;padding:3px 4px;
  border:1px solid var(--border);border-radius:6px;
  background:var(--surf);color:var(--ink-m);
}
.video-thumb{
  display:flex;align-items:center;justify-content:center;
  width:120px;height:80px;border-radius:10px;
  background:var(--surf3);font-size:.7rem;color:var(--muted-l);
}
```

---

## 执行顺序

1. **改动 1**：`backend/routers/assets.py` — 扩展上传接口（加字段）
2. **改动 2**：`backend/services/service_manager.py` — 新增 `_select_reference_photo()` + 修改 `gen_scene_image()`
3. **改动 3**：`backend/routers/pipeline.py` — 路由接收参数
4. **改动 5.1**：`backend/routers/assets.py` — 新增 `/update-meta` 接口
5. **改动 7**：`frontend/css/style.css` — 新增样式
6. **改动 4**：`frontend/memorial.html` — 修改上传区域 HTML
7. **改动 5**：`frontend/js/memorial.js` — 重写 `handleUpload()`
8. **改动 6**：`frontend/js/studio.js` — 修改 `genSceneImage()`

---

## 验证方法

1. 在 `memorial.html` Step 2 上传一张逝者青年时期照片，选择"逝者本人"+"青年时期"
2. 上传一张中年工作照，选择"逝者本人"+"中年时期"
3. 上传一张合照，选择"合照/多人"
4. 进入 pipeline.html → studio 页面，完成 MV04 分镜生成
5. 点击某个分镜的"生成图片"按钮
6. 检查生成的图片是否参考了对应时期的照片
7. 检查 `session` 中 `scenes[i]["_reference_photo"]` 字段记录了使用的照片文件名
8. 确认视频文件没有被用作参考（只应取 .jpg/.png 等图片文件）
